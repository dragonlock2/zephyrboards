if SOC_SERIES_LPC84X

source "soc/arm/nxp_lpc/lpc84x/Kconfig.defconfig.lp*"

config SOC_SERIES
    default "lpc84x"

config NUM_IRQS
    # must be >= the highest interrupt number used
    default 40

endif # SOC_SERIES_LPC84X
