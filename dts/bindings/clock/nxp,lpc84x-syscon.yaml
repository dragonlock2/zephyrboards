description: LPC84X SYSCON node

compatible: "nxp,lpc84x-syscon"

include: [clock-controller.yaml, base.yaml]

properties:
    reg:
      required: true

    "#clock-cells":
      const: 1

clock-cells:
  - name
