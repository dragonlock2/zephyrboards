description: LPC84X UART node

compatible: "nxp,lpc84x-uart"

include: uart-controller.yaml

properties:
  reg:
    required: true

  swm:
    type: array
    required: true

  clk:
    type: array
    required: true

  pinctrl:
    type: array
    required: true
